[pytest]
# Run from the project root so `import config` and `import src...` resolve.
pythonpath = .
testpaths = tests
addopts = -q
markers =
    network: tests that hit live market-data APIs (skipped automatically when offline)
